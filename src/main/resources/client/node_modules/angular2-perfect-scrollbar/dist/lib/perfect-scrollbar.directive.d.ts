import { DoCheck, OnDestroy, ElementRef, AfterViewInit, NgZone } from '@angular/core';
import { PerfectScrollbarConfig, PerfectScrollbarConfigInterface } from './perfect-scrollbar.interfaces';
export declare class PerfectScrollbarDirective implements DoCheck, OnDestroy, AfterViewInit {
    elementRef: ElementRef;
    private defaults;
    private zone;
    private width;
    private height;
    private contentWidth;
    private contentHeight;
    runInsideAngular: boolean;
    config: PerfectScrollbarConfigInterface;
    constructor(elementRef: ElementRef, defaults: PerfectScrollbarConfig, zone: NgZone);
    ngDoCheck(): void;
    ngOnDestroy(): void;
    ngAfterViewInit(): void;
    update(): void;
    scrollTo(x: number, y?: number): void;
    scrollToTop(offset?: number): void;
    scrollToLeft(offset?: number): void;
    scrollToRight(offset?: number): void;
    scrollToBottom(offset?: number): void;
}
